import { PrimerDirective } from './primer.directive';

describe('PrimerDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimerDirective();
    expect(directive).toBeTruthy();
  });
});
